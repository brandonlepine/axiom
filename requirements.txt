# Core numerics / data
pandas
numpy
pyarrow
fastparquet
tqdm

# Config + validation
pydantic>=2.5
PyYAML

# Modeling
# Versions below are the locally-validated combination (GPT-2 smoke test, MPS). Re-pin
# exact versions from the pod environment once the first CUDA run is confirmed.
#
# POD WARNING: do NOT blanket `pip install -r requirements.txt` on a GPU pod -- the
# default-PyPI `torch` wheel can clobber the pod's driver-matched build and break CUDA
# (symptoms: "NVIDIA driver too old" -> silent CPU fallback; "torchvision::nms does not
# exist" masquerading as a BERT import error). On the pod, install a torch wheel matching
# the driver's CUDA (see `nvidia-smi`), e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# then install the rest below. We do NOT use torchvision; if it is present-but-broken,
# `pip uninstall -y torchvision`. See docs/PIPELINE.md "Troubleshooting".
torch>=2.1
transformers>=4.44
accelerate>=0.33
tokenizers>=0.19
huggingface_hub
safetensors
transformer_lens>=3.3

# Analysis
scikit-learn

# Figures (publication / colorblind-safe styling lives in axiom.figures)
matplotlib
seaborn
cycler

# Testing
pytest
